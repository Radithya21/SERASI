<!DOCTYPE html>
<html lang="id">
<head>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style> body { font-family: 'Inter', sans-serif; } </style>
</head>
<body class="bg-gray-100 font-sans antialiased">
<div class="flex min-h-screen">
    
    <% include ../partials/sidebarAdmin %>

    <div class="flex-1 p-6">
    <div class="container mx-auto bg-white p-6 rounded-lg shadow-md">
        <h1 class="text-3xl font-bold mb-6 text-gray-800"><%= title %></h1>

        <% if (success_msg && success_msg.length > 0) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline"><%= success_msg %></span>
        </div>
        <% } %>
        <% if (error_msg && error_msg.length > 0) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline"><%= error_msg %></span>
        </div>
        <% } %>

        <% if (arsip.length === 0) { %>
        <p class="text-gray-500">Belum ada notulensi yang diarsipkan.</p>
        <% } else { %>
        <div class="overflow-x-auto">
            <table class="table-auto w-full border mt-4">
            <thead>
                <tr class="bg-gray-200 text-left">
                    <th class="p-2 border">Tanggal</th>
                    <th class="p-2 border">Judul</th>
                    <th class="p-2 border">Deskripsi</th>
                    <th class="p-2 border">File</th>
                </tr>
            </thead>
            <tbody>
                <% arsip.forEach(n => { %>
                <tr class="hover:bg-gray-50">
                    <td class="p-2 border"><%= new Date(n.tanggal).toLocaleDateString('id-ID') %></td>
                    <td class="p-2 border"><%= n.judul || '-' %></td>
                    <td class="p-2 border"><%= n.deskripsi || '-' %></td>
                    <td class="p-2 border">
                    <% if (n.file) { %>
                        <a href="/uploads/<%= n.file %>" class="text-blue-600 underline">Unduh</a>
                    <% } else { %>
                        <span class="text-gray-400">Belum ada file</span>
                    <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
            </table>
        </div>
        <% } %>
    </div>
    </div>
</div>
</body>
</html>