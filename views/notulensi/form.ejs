<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-100 font-sans antialiased">
    <div class="flex min-h-screen">
        <!-- Sidebar Admin -->
        <% include ../partials/sidebarAdmin %>

        <div class="flex-1 p-6">
            <div class="container mx-auto bg-white p-6 rounded-lg shadow-md">
                <h1 class="text-3xl font-bold mb-6 text-gray-800"><%= title %></h1>

                <% if (success_msg && success_msg.length > 0) { %>
                    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
                        <span class="block sm:inline"><%= success_msg %></span>
                    </div>
                <% } %>
                <% if (error_msg && error_msg.length > 0) { %>
                    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
                        <span class="block sm:inline"><%= error_msg %></span>
                    </div>
                <% } %>

                <form action="<%= isEdit ? '/notulensi/edit/' + notulensi.id_notulen : '/notulensi/new' %>" method="POST" class="space-y-4">
                    <div>
                        <label for="rapatId" class="block text-sm font-medium text-gray-700">Pilih Rapat</label>
                        <select id="rapatId" name="rapatId"
                            class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                            <%= isEdit ? 'disabled' : 'required' %>>
                            <option value="">-- Pilih Rapat --</option>
                            <% rapatList.forEach(rapat => { %>
                                <option value="<%= rapat.id_rapat %>"
                                    <%= (notulensi && notulensi.id_rapat === rapat.id_rapat) ? 'selected' : '' %>>
                                    <%= rapat.judul %> (<%= new Date(rapat.tanggal).toLocaleDateString('id-ID') %>)
                                </option>
                            <% }); %>
                        </select>
                        <% if (isEdit) { %>
                            <p class="mt-1 text-sm text-gray-500">Rapat tidak dapat diubah setelah notulensi dibuat.</p>
                        <% } %>
                    </div>

                    <div>
                        <label for="dokumentasi" class="block text-sm font-medium text-gray-700">Dokumentasi (Nama File)</label>
                        <input type="file" id="dokumentasi" name="dokumentasi"
                            class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                            placeholder="Contoh: FotoRapat.png (Tidak disimpan langsung ke notulensi ini)"
                            value="<%= (notulensi && notulensi.rapat && notulensi.rapat.dokumen && notulensi.rapat.dokumen.length > 0) ? notulensi.rapat.dokumen[0].nama_file : '' %>"
                            >
                        <p class="mt-1 text-sm text-gray-500">Catatan: Pengelolaan dokumen dilakukan melalui modul Dokumen Rapat. Ini hanya tampilan.</p>
                    </div>
                    
                    <div>
                        <label for="isi_notulen" class="block text-sm font-medium text-gray-700">Isi Notulen</label>
                        <textarea id="isi_notulen" name="isi_notulen" rows="10"
                            class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-blue-500 focus:border-blue-500"
                            required><%= notulensi ? notulensi.isi_notulen || '' : '' %></textarea>
                    </div>

                    <!-- Hidden input for status, default to 'draft' or current status -->
                    <input type="hidden" name="status" id="notulensiStatus" value="<%= notulensi ? notulensi.status : 'draft' %>">

                    <div class="flex justify-end space-x-3">
                        <a href="/notulensi"
                            class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md">Batal</a>
                        
                        <% if (!isEdit) { %>
                            <button type="submit" onclick="document.getElementById('notulensiStatus').value='draft';"
                                class="bg-orange-500 hover:bg-orange-600 text-white font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md">Draft</button>
                            <button type="submit" onclick="document.getElementById('notulensiStatus').value='uploaded';"
                                class="bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md">Unggah</button>
                        <% } else { %>
                            <button type="submit" onclick="document.getElementById('notulensiStatus').value='draft';"
                                class="bg-orange-500 hover:bg-orange-600 text-white font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md">Simpan Draft</button>
                            <button type="submit" onclick="document.getElementById('notulensiStatus').value='uploaded';"
                                class="bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md">Unggah</button>
                        <% } %>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>