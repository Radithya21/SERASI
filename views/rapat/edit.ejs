<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Rapat</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
</head>

<body class="bg-gray-100 font-sans antialiased">
    <div class="flex min-h-screen">
        <% include ../partials/sidebarAdmin %> <!-- Sidebar di sisi kiri -->

            <div class="flex-1 p-6">
                <div class="container mx-auto bg-white p-6 rounded-lg shadow-md">

                    <!-- Judul Halaman -->
                    <h1 class="text-3xl font-bold mb-6 text-gray-800">Edit Rapat: <%= rapat.judul %>
                    </h1>

                    <!-- Form Edit Rapat -->
                    <form action="/rapat/edit/<%= rapat.id_rapat %>" method="POST">
                        <div class="mb-4">
                            <label for="judul" class="font-semibold text-gray-800">Judul Rapat:</label>
                            <input type="text" id="judul" name="judul"
                                class="mt-1 p-2 w-full border border-gray-300 rounded-md" value="<%= rapat.judul %>"
                                required>
                        </div>

                        <div class="mb-4">
                            <label for="tanggal" class="font-semibold text-gray-800">Tanggal Rapat:</label>
                            <!-- Menggunakan input type date untuk memilih tanggal -->
                            <input type="date" id="tanggal" name="tanggal"
                                class="mt-1 p-2 w-full border border-gray-300 rounded-md"
                                value="<%= new Date(rapat.tanggal).toISOString().split('T')[0] %>" required>
                        </div>

                        <div class="mb-4">
                            <label for="tempat" class="font-semibold text-gray-800">Tempat Rapat:</label>
                            <input type="text" id="tempat" name="tempat"
                                class="mt-1 p-2 w-full border border-gray-300 rounded-md" value="<%= rapat.tempat %>"
                                required>
                        </div>

                        <div class="mb-4">
                            <label for="deskripsi" class="font-semibold text-gray-800">Deskripsi:</label>
                            <textarea id="deskripsi" name="deskripsi"
                                class="mt-1 p-2 w-full border border-gray-300 rounded-md"
                                rows="4"><%= rapat.deskripsi %></textarea>
                        </div>

                        <!-- Button untuk mengirimkan data -->
                        <div class="mt-6 flex space-x-4">
                            <button type="submit"
                                class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-md transition duration-300">
                                Update Rapat
                            </button>
                            <a href="/rapat"
                                class="bg-gray-500 hover:bg-gray-600 text-white py-2 px-4 rounded-md transition duration-300">
                                Kembali
                            </a>
                        </div>
                    </form>
                </div>
</body>

</html>