<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - SERASI</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
</head>

<body class="bg-gradient-to-br from-orange-50 via-yellow-50 to-orange-100 min-h-screen flex items-center justify-center p-4">
    <div class="container max-w-md w-full">
        <!-- Error Card -->
        <div class="bg-white/90 backdrop-blur-sm rounded-2xl shadow-2xl overflow-hidden border border-orange-200/50">
            <!-- Header dengan icon error -->
            <div class="bg-gradient-to-r from-red-500 via-red-600 to-red-700 p-6 text-center relative overflow-hidden">
                <!-- Decorative elements -->
                <div class="absolute top-0 left-0 w-full h-full">
                    <div class="absolute top-4 right-4 w-16 h-16 bg-white/10 rounded-full"></div>
                    <div class="absolute bottom-4 left-4 w-12 h-12 bg-white/10 rounded-full"></div>
                </div>
                
                <div class="relative z-10">
                    <div class="mb-4">
                        <div class="w-20 h-20 mx-auto bg-white/20 rounded-full flex items-center justify-center mb-3 backdrop-blur-sm">
                            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
                            </svg>
                        </div>
                    </div>
                    <h1 class="text-2xl font-bold text-white mb-2 drop-shadow-lg">Oops!</h1>
                    <p class="text-red-100 text-sm font-medium leading-relaxed">
                        Terjadi kesalahan pada sistem
                    </p>
                </div>
            </div>

            <!-- Error Content -->
            <div class="p-6">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-bold text-gray-800 mb-2">Error <%= error.status %></h2>
                    <p class="text-gray-600 text-sm mb-4"><%= message %></p>
                </div>

                <!-- Error Details (only in development) -->
                <% if (process.env.NODE_ENV === 'development') { %>
                    <div class="bg-gray-50 rounded-lg p-4 mb-6">
                        <h3 class="text-sm font-semibold text-gray-700 mb-2">Error Details:</h3>
                        <pre class="text-xs text-gray-600 overflow-x-auto whitespace-pre-wrap"><%= error.stack %></pre>
                    </div>
                <% } %>

                <!-- Action Button -->
                <div class="mb-6">
                    <a href="/" 
                        class="w-full bg-orange-500 hover:bg-orange-600 text-white font-semibold py-2.5 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl shadow-lg flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                        </svg>
                        Kembali ke Beranda
                    </a>
                </div>

                <!-- Footer -->
                <div class="text-center pt-4 border-t border-gray-200">
                    <p class="text-xs text-gray-500 font-medium">
                        SERASI Sistem Informasi Universitas Andalas 2025
                    </p>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
